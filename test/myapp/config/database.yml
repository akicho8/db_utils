defaults: &defaults
  adapter: mysql2
  username: root
  password:
  encoding: utf8
  host: 127.0.0.1
  reconnect: true

development:
  <<: *defaults
  database: _myapp_development

test:
  <<: *defaults
  database: _myapp_test
